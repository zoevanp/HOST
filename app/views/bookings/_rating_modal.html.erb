
<div class="modal fade" id="ratingModal" tabindex="-1" aria-labelledby="ratingModalLabel" aria-hidden="true" data-controller="rating">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="ratingModalLabel">Did you had a good time with <%= @booking.refugee.email%>?</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
      <div class="container">
        <div class="row justify-content-center">
        <div class="rating-text">
             <p>This rating is never going to be displayed. It is only for organization purposes.</p>
             <p>The people with the best ratings will get rooms assigned first.</p>
        </div>
           <div class="col-12 col-lg-5 pt-3">
             <%= simple_form_for [@booking, @review] do |f|%>
             <%= f.input :rating, collection: (1..5).to_a, label: "Rate your experience", input_html: {data: {controller: "star-rating"}}, label_html: {class: "text-center w-100"} %>
             <%= f.input :description, placeholder: 'Write your feedback here' %>
             <%= f.button :submit, "Submit", class: "universal-button text-center w-100" %>
             <% end %>
          </div>
        </div>
      </div>
      </div>
  </div>
</div>
